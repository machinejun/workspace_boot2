- 한 대의 컴퓨터에 여러가지 통신을 하고 싶다면 포트가 필요하다.
- 포트번호 = 0 ~ 65535 번 까지 정해져 있다.
- 운영체제는 모든 포트를 막아놓았는데 >>>> 액세스 허용하겠다라고 하면 포트를 연다

0 ~ 1023 : 정해진 포트(건드릴 수 없다.)
그 이상의 포트 번호는 어느 것이든 사용 할 수 있다.
1024 ~ 65535: 사용하지 않는 포트번호를 랜덤으로 생성




소켓 통신이란
: 양 끝단에 포트를 달아서 "byte 스트림"을 통해서 통신을 주고 받는 것
: 메세지를 주고 받고 싶다면 >> 버퍼가 필요함

서버는 2개의 소켓이 필요하다.

1번 소켓: 
2번 소켓:  서버 소켓은 연결만 받는다.


IP주소: 네트워크 상에서 다른 PC에 접속하기 위해서, PC를 가리키는 주소

IPv4 : 

Http 통신: 근간은 소켓통신      >>>>>>>>>>>>>>>>>>> 차이점: http통신( state less 서버) : 한번 응답을 하고나면 통신을 끊는다. 
                                                                                         but 소켓(state full server)은 연결을 종료할 때까지 연결되어 있다.

네이버 서버                                         브라우저

웹 소켓       <---------------------------연결 요청
(input buffer)              
      html코드  -------------------------> byte Stream 응답 처리
                                                   html 응답  ---> 버퍼를 통해 받아서 repaint
                                                               